---
title: "Creating a Wallet"
sidebar_label: "Creating a Wallet"
slug: /CreatingWallet
hide_title: false
hide_table_of_contents: true
sidebar_position: 20
description: ""
---

import React from 'react';

import useBaseUrl from '@docusaurus/useBaseUrl';

import TestnetNote from '@site/src/components/TestnetNote.js';

import CodeBlock from '@theme/CodeBlock';
import createWallet from '!!raw-loader!/txt/createWallet.sh';
import utxo from '!!raw-loader!/txt/utxo.json';

export function HTMLContent() {
  return (
    <div>

      {/* INSERT HTML HERE */}

      <p>To operate a stake pool, you need to create the following addresses for a wallet:</p>

      <ul>
        <li><b>Payment Address</b>—Used to store, receive and send money</li>
        <li><b>Stake Address</b>—Used to store and withdraw rewards, as well as to define the stake pool owner, rewards accounts and stake pool delegation</li>
      </ul>

      <p>You can create a wallet for your stake pool using key pairs or a seed phrase. While creating a wallet using keys is simpler, using a seed phrase offers the convenience of accessing the rewards account holding the pledge for your stake pool using an online, Desktop or Mobile <a href="https://docs.cardano.org/about-cardano/new-to-cardano/types-of-wallets" target="_blank">wallet</a>.</p>

      <table class="box">
        <tbody>
          <tr>
            <td class="boxLabel"><p class="boxLabel">Note<br /><img src={useBaseUrl('img/BoxNote.png')} alt="Note" /></p></td>
            <td class="boxText"><p class="boxText">Pledge is the stake that you delegate to the pool that you operate, serving as a security measure and indicating your commitment to the Cardano ecosystem.</p></td>
          </tr>
        </tbody>
      </table>

      <p>If you create a wallet using a seed phrase, then you can generate the keys for the wallet. If you create a wallet using keys, then generating the seed phrase for the wallet is NOT possible.</p>

      <p>For security, generate key pairs, addresses and the optional seed phrase using your air-gapped, offline computer.</p>

      <TestnetNote />

      <h1>Using Keys</h1>

      <p class="pi">To create a new wallet using a terminal window on your air-gapped, offline computer:</p>

      <ol>
        <li>To generate a new payment address key pair using the file names <code>payment.vkey</code> and <code>payment.skey</code> type:
          <CodeBlock language="bash">
            {`cd $NODE_HOME
cardano-cli conway address key-gen \\
  --verification-key-file payment.vkey \\
  --signing-key-file payment.skey`}
          </CodeBlock>
          <table class="box">
            <tbody>
              <tr>
                <td class="boxLabel"><p class="boxLabel">Note<br /><img src={useBaseUrl('img/BoxNote.png')} alt="Note" /></p></td>
                <td class="boxText"><p class="boxText">You copied the Cardano CLI binary when <a href="/OfflineComputer">Configuring an Air-gapped, Offline Computer</a>.</p></td>
              </tr>
            </tbody>
          </table>
        </li>
        <li>To generate a new stake address key pair using the file names <code>stake.vkey</code> and <code>stake.skey</code> type:
          <CodeBlock language="bash">
            {`cardano-cli conway stake-address key-gen \\
  --verification-key-file stake.vkey \\
  --signing-key-file stake.skey`}
          </CodeBlock>
        </li>
        <li>To generate a stake address using the file name <code>stake.addr</code> type:
          <CodeBlock language="bash">
            {`cardano-cli conway stake-address build \\
  --stake-verification-key-file stake.vkey \\
  --out-file stake.addr \\
  --mainnet`}
          </CodeBlock>
        </li>
        <li>To generate a payment address using the file name <code>payment.addr</code> that is associated with the stake address you created in step 3, type:
          <CodeBlock language="bash">
            {`cardano-cli conway address build \\
  --payment-verification-key-file payment.vkey \\
  --stake-verification-key-file stake.vkey \\
  --out-file payment.addr \\
  --mainnet`}
          </CodeBlock>
        </li>
        <li>Carefully back up the files containing the payment address and stake address as well as the corresponding public verification and secret keys using a secure and comprehensive backup strategy.</li>
        <li>Using removable media, copy the <code>stake.vkey</code>, <code>stake.addr</code> and <code>payment.addr</code> files to the <code>$NODE_HOME</code> folder on your block-producing node.</li>
      </ol>

      <a id="SeedPhrase" />
      <h1>Using a Seed Phrase</h1>

      <p class="pi">To create a new wallet using a seed phrase:</p>

      <ol>
        <li>Using a terminal window on the computer designated as the block-producing node in your stake pool configuration, type the following commands to download the latest release of <a href="https://github.com/cardano-foundation/cardano-wallet" target="_blank">Cardano Wallet</a>:
          <CodeBlock language="bash">
            {`cd $NODE_HOME
RELEASETAG=$(curl -s https://api.github.com/repos/cardano-foundation/cardano-wallet/releases/latest | jq -r .tag_name)
wget https://github.com/cardano-foundation/cardano-wallet/releases/download/$\{RELEASETAG\}/cardano-wallet-$\{RELEASETAG\}-linux64.tar.gz`}
          </CodeBlock>
        </li>
        <li>Using removable media, copy the TAR.GZ archive that you downloaded in step 1 to the <code>$NODE_HOME</code> folder on your air-gapped, offline computer, and then type the following command using a terminal window to extract the contents of the archive, where <code>&lt;ArchiveName&gt;</code> is the name of the file containing Cardano Wallet:
          <CodeBlock language="bash">
            {`tar -xvzf <ArchiveName>`}
          </CodeBlock>
        </li>
        <li>To copy the <code>shelley-genesis.json</code> file for the environment that you use, type the following command where <code>&lt;ArchiveFolder&gt;</code> is the name of the folder where you extracted Cardano Wallet in step 2, and <code>&lt;EnvironmentName&gt;</code> is the name of the environment:
          <CodeBlock language="bash">
            {`cp ./<ArchiveFolder>/configs/<EnvironmentName>/shelley-genesis.json $NODE_HOME`}
          </CodeBlock>
        </li>
        <li>To add Cardano Wallet as well as bundled Cardano CLI binaries to the PATH environment variable temporarily, and then generate a 24-word seed phrase, type:
          <CodeBlock language="bash">
            {`export PATH="$(pwd)/cardano-wallet-$\{RELEASETAG\}-linux64:$PATH"
cardano-wallet recovery-phrase generate --size 24 > seed-phrase.dat`}
          </CodeBlock>
        </li>

        <li>Using a text editor, create a new file named <code>$NODE_HOME/createWallet.sh</code>, and then paste the following script into the file:
          <CodeBlock language="bash">
            {createWallet}
          </CodeBlock>
          {/* <!-- For more details on the Cardano derivation path, see https://cardano.stackexchange.com/questions/7671/what-is-the-derivation-path-in-a-cardano-address --> */}
          <table class="box">
            <tbody>
              <tr>
                <td class="boxLabel"><p class="boxLabel">Credit<br /><img src={useBaseUrl('img/BoxCredit.png')} alt="Credit" /></p></td>
                <td class="boxText"><p class="boxText">Thanks to <a href="https://geldtiger.gitbook.io/geti-explainer/create-cardano-wallet" target="_blank">Geld Tiger</a>, <a href="https://gist.github.com/den-is/0fb758b07cdb68abc968b3dea689fa9b" target="_blank">den-is</a> and <a href="https://gist.github.com/ilap/3fd57e39520c90f084d25b0ef2b96894" target="_blank">ilap</a> for preparing the original script.</p></td>
              </tr>
            </tbody>
          </table>
        </li>
        <li>Save and close the <code>createWallet.sh</code> file, and then type the following command to grant file permissions for executing the script:
          <CodeBlock language="bash">
            {`chmod +x createWallet.sh`}
          </CodeBlock>
        </li>
        <li>To generate keys and addresses for your wallet, type:
          <CodeBlock language="bash">
            {`./createWallet.sh 0 wallet-keys $(cat seed-phrase.dat)`}
          </CodeBlock>
        </li>
        <li>To prevent overwriting your seed phrase, type the following command to remove Cardano Wallet binaries where <code>&lt;ArchiveFolder&gt;</code> is the name of the folder where you extracted Cardano Wallet in step 2:
          <CodeBlock language="bash">
            {`rm -r $NODE_HOME/<ArchiveFolder>`}
          </CodeBlock>
        </li>
        <li>To protect your seed phrase, type the following command to clear history:
          <CodeBlock language="bash">
            {`history -c && history -w`}
          </CodeBlock>
        </li>
        <li>Close the terminal window that you used to create your wallet, and then open a new terminal window if needed.</li>
        <li>Carefully back up the <code>$NODE_HOME/seed-phrase.dat</code> file, as well as all files in the <code>$NODE_HOME/wallet-keys/</code> folder, using a secure and comprehensive backup strategy.</li>
        <li>Using removable media, from the <code>$NODE_HOME/wallet-keys/</code> folder copy the <code>stake.vkey</code>, <code>stake.addr</code> and <code>payment.addr</code> files to the <code>$NODE_HOME</code> folder on the computer designated as the block-producing node in your stake pool configuration.</li>
      </ol>

      <a id="FundingAddress" />
      <h1>Funding Your Payment Address</h1>

      <p>Using a wallet, send funds that you want to pledge to your stake pool from another account you control to the new payment address.</p>

      <p class="pi">To fund the new payment address:</p>

      <ol>
        <li>To display the new payment address, using a terminal window type:
          <CodeBlock language="bash">
            {`cat $NODE_HOME/payment.addr`}
          </CodeBlock>
        </li>
        <li>For a Mainnet payment address, use a wallet of your choice to send funds that you want to pledge to the address that displays in step 1
          <center><strong>OR</strong></center>
          For a payment address in a Testnet environment, request funds using the <a href="https://docs.cardano.org/cardano-testnets/tools/faucet" target="_blank">Testnets Faucet</a>.
          <table class="box">
            <tbody>
              <tr>
                <td class="boxLabel"><p class="boxLabel">Challenge<br /><img src={useBaseUrl('img/BoxChallenge.png')} alt="Challenge" /></p></td>
                <td class="boxText"><p class="boxText">Using Cardano CLI, create a transaction <a href="/SendingFunds">Sending Funds</a> to the new payment address.</p></td>
              </tr>
            </tbody>
          </table>
        </li>
        <li>Wait a few moments for the transaction to complete. To verify the transaction, type the following command using a terminal window on the block-producing node to display the list of Unspent Transaction Output (UTXOs) available for the new payment address, and then find the UTXO for the funds that you sent in step 2:
          <CodeBlock language="bash">
            {`cardano-cli conway query utxo --output-json \\
  --address $(cat $NODE_HOME/payment.addr) \\
  --mainnet`}
          </CodeBlock>
        </li>
      </ol>

      <p>The UTXO query displays JSON output similar to the following—where the key is the transaction hash, followed by a hash (<code>#</code>) character, and then followed by the transaction index—displaying the value of the UTXO in Lovelace:</p>

      <CodeBlock language="json">
        {utxo}
      </CodeBlock>

      <table class="box">
        <tbody>
          <tr>
            <td class="boxLabel"><p class="boxLabel">Note<br /><img src={useBaseUrl('img/BoxNote.png')} alt="Note" /></p></td>
            <td class="boxText"><p class="boxText">Named after the English mathematician and writer Ada Lovelace, the currency symbol for ADA is ₳ and the sub-unit is the Lovelace. ₳1 is comprised of 1,000,000 Lovelace.</p></td>
          </tr>
        </tbody>
      </table>

    </div>
  );
}

<HTMLContent />

