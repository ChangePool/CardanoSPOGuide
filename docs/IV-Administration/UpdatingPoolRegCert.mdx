---
title: "Updating the Stake Pool Registration Certificate"
sidebar_label: "Updating the Stake Pool Registration Certificate"
slug: /UpdatingPoolRegCert
hide_title: false
hide_table_of_contents: true
sidebar_position: 15
description: ""
---

import React from 'react';

export function HTMLContent() {
  return (
    <div>

      {/* INSERT HTML HERE */}

      <p>The most recent stake pool registration certificate that you submit to the Cardano blockchain ledger applies to your stake pool.</p>

      <p>Some changes that you make to the registration certificate for your stake pool may take up to two epochs to apply.</p>
      
      <p class="pi">To update details related to the registration of your stake pool:</p>

      <ol>
        <li>Prepare and publish updated metadata.</li>
        <li>Generate an updated stake pool registration certificate.</li>
        <li>If needed, generate an updated stake delegation certificate.</li>
        <li>Submit the updated stake pool registration certificate—and stake delegation certificate, if applicable—to the blockchain.
          <table class="box">
            <tbody>
              <tr>
                <td class="boxLabel"><p class="boxLabel">Note<br /><img src="img/BoxNote.png" alt="Note" /></p></td>
                <td class="boxText"><p class="boxText">You pay the deposit required to register a stake pool only when registering a stake pool—NOT when updating the stake pool registration certificate. If you do not include a stake delegation certificate in a transaction updating the stake pool registration certificate, then the transaction still requires the <code>payment.skey</code>, <code>node.skey</code> and <code>stake.skey</code> signing key files.</p></td>
              </tr>
            </tbody>
          </table>
        </li>
      </ol>

      <p>For more details, see the topic <a href="/RegisteringPool">Registering a Stake Pool</a>.</p>



    </div>
  );
}

<HTMLContent />

