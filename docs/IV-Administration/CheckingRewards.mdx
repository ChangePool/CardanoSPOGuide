---
title: "Checking Stake Pool Rewards"
sidebar_label: "Checking Stake Pool Rewards"
slug: /CheckingRewards
hide_title: false
hide_table_of_contents: true
sidebar_position: 20
description: ""
---

import React from 'react';

import useBaseUrl from '@docusaurus/useBaseUrl';

import TestnetNote from '@site/src/components/TestnetNote.js';

import CodeBlock from '@theme/CodeBlock';
import queryStakeAddressInfo from '!!raw-loader!/txt/queryStakeAddressInfo.sh';

export function HTMLContent() {
  return (
    <div>

      {/* INSERT HTML HERE */}

      <p>At the end of the next epoch, the Cardano protocol pays stake pools and delegators rewards for blocks produced in the current epoch.</p>

      <TestnetNote />

      <p class="pi">To display the current stake pool and vote delegations, as well as the reward balance for a stake address:</p>
      
      <ul class="ss">
        <li>Using a terminal window on the block-producing node in your stake pool configuration, type:
          <CodeBlock language="bash">
            {queryStakeAddressInfo}
          </CodeBlock>
          <center><strong>OR</strong></center>
          Search for the stake address using a <a href="https://explorer.cardano.org/" target="_blank">blockchain explorer</a>.
        </li>
      </ul>

      <p>The <code>query stake-address-info</code> command returns a JSON object. For example:</p>

      <CodeBlock language="json">
        {`[
  {
    "address": "stake_test1uz2euqqvaf0v75yhwayefqx0wu0g7jyd0grjzhvkgcmv3ag5avecv",
    "delegationDeposit": 2000000,
    "rewardAccountBalance": 39019220657,
    "stakeDelegation": "pool1ja5uhsx8cmda7x6sqyzrggj07qtncx22lxef398u3ayusfex629",
    "voteDelegation": null
  }
]`}
      </CodeBlock>

      <table class="box">
        <tbody>
          <tr>
            <td class="boxLabel"><p class="boxLabel">Note<br /><img src={useBaseUrl('img/BoxNote.png')} alt="Note" /></p></td>
            <td class="boxText"><p class="boxText">For details on vote delegations, see the topic <a href={useBaseUrl('/VoteDelegation')}>Delegating to a Representative</a>.</p></td>
          </tr>
        </tbody>
      </table>

    </div>
  );
}

<HTMLContent />

